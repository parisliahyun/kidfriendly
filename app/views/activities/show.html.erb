<div class="well">
  <h4></h4>
  <ul>
    <li>Image Url: <%= @activity.image_url %></li>
    <li>Description: <%= @activity.description %></li>
    <li>Tags: <%= @activity.tag_list %></li>
    <li>Price: $<%= @activity.price %>0</li>
    <li>This activity is taught by: <%= @vendor.name %></li>
    <li><%= link_to "View available dates", user_schedules_path(@activity.vendor_id)%></li>
    <% if current_user.id == @activity.vendor_id %>
    <li><%= link_to "Edit this activity", edit_activity_path(@activity) %></li>
    <% end %>
  </ul>

  Rating : <%= rating_for @activity %>
  <h2>Comments</h2>

  <%= render "comments/comments" %>
  <%= render "comments/form" %>
</div>